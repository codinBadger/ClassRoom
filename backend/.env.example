# Example environment configuration
# Copy this file to .env and update with your actual values

# Database
DATABASE_URL=sqlite:classroom.db

# JWT Secret - Generate a strong random secret for production
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-this-in-production

# Server Configuration
RUST_LOG=info
HOST=0.0.0.0
PORT=8080

# SECURITY NOTES:
# 1. Never commit the actual .env file to version control
# 2. Use strong, randomly generated secrets in production
# 3. Implement proper code execution sandboxing before deploying
# 4. Consider using environment-specific secrets management (e.g., AWS Secrets Manager, HashiCorp Vault)
